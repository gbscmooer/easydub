# EasyDub GOAL — 项目目标与验收规格 v1.0

> 双目标文档：**本科毕业设计**（答辩验收）+ **求职作品**（AI 应用开发 / 大模型应用工程岗）。
> 每条目标可量化、可演示、可写进论文或简历。定稿：2026-09-05。

---

## 1. 一句话目标

把一条**中文口播视频（MP4）**变成一条**外语成品视频（MP4）**：目标语配音替换原声、
中外双语字幕、人脸段口型同步；全流程一条命令（CLI）或一次网页拖拽（Web）完成，
并自动产出逐段量化的对齐质量报告。

---

## 2. 输入 / 输出规格（具体）

### 2.1 输入（本项目处理的范围）

- **格式**：MP4 / MOV，H.264，分辨率 720p 或 1080p，帧率 24–60fps
- **时长**：10 秒 – 5 分钟；文件 ≤ 200MB；ASR 分段数 ≤ 200 段
- **语音**：中文普通话，1–2 名说话人（**同一时刻只有一人在说话**），人声清晰
  （允许低音量 BGM，但人声必须可懂）
- **内容类型**：口播广告、vlog、教程讲解等"人对着镜头说话 / 旁白"类视频

### 2.2 输出（成品规格）

1. **成品视频** `xxx.dub.<lang>.mp4`
   - 分辨率 / 帧率与输入一致；H.264 + AAC 48kHz；
   - 音轨 = 目标语配音（**替换原声**，不做 BGM 保留混音）；
   - 字幕 = 中外双语**软字幕轨**（mov_text，播放器可开关）+ 独立 `.srt` 文件；
     ffmpeg 带 libass 时升级为烧录。
2. **逐段中间产物**（`workdir/<视频名>/`，可断点续跑）：
   `segments.json`（ASR）→ `segments_translated.json` → `segments_final.json`（含 tempo/action）、
   `tts/*.mp3`、`dub_track.wav`
3. **`report.json` 质量报告**：逐段 `slot / tts时长 / tempo / action` + 汇总
   （fit / atempo / overflow 计数、重译前后溢出对比）
4. **Web 端**：提交任务 → 进度可查 → 浏览器内播放成品视频

### 2.3 支持目标语言

`en`（主验证语言）；`ja`、`es` 各 1 条做泛化抽测。

---

## 3. 技术栈（已选定，不再摇摆）

| 层 | 选型 |
|----|------|
| 运行时 | Python 3.13（venv） |
| ASR | faster-whisper（本地）+ OpenRouter 云端 ASR，适配器切换 |
| 翻译 | OpenAI / Anthropic 双协议 LLM（DeepSeek / OpenRouter）；限长创译提示词 + 溢出重译闭环 |
| TTS | fish-audio s2.1-pro（OpenRouter，主）/ edge-tts（免费后备）/ Minimax 适配器备用 |
| 时长对齐 | 纯函数 `plan_alignment` + ffmpeg atempo（限幅 1.25）+ 溢出重译 |
| 媒体处理 | ffmpeg ≥ 8.0，唯一出口 `media.py`（extract / adelay / amix / atempo / subtitle / mux） |
| lip-sync | LatentSync，自建 Windows RTX 5090 推理服务（FastAPI :8001）；sync.so 免费额度仅做对比实验 |
| 产品面 | FastAPI 三端点（`/api/jobs`）+ SQLite 任务表 + React 拖拽上传页（Vite，Node ≥ 20） |
| 编排 | 各阶段注册为 MCP 工具 + LLM Agent 决策重译；Dify Cloud workflow（毕设演示可选） |
| 测试 | pytest 纯逻辑单测（无网络依赖）+ 评测脚本（§6 指标自动计算） |

---

## 4. 项目边界

### 4.1 要处理

- 中文普通话口播 → en / ja / es 翻译配音
- **音画同步**（核心课题）：译文超时的限长、变速、重译
- 人脸出镜段的口型同步；空镜 / 产品镜头自动跳过（人脸检测分段）
- 断点续跑：每阶段产物落盘缓存，重跑只补失败阶段
- 异常路径：无声视频正常完成（零段落）、API 失败重试、超长段溢出记录

### 4.2 不处理（明确出界，答辩被问直接认）

| 不做 | 原因 |
|------|------|
| 说话人分离（diarization）/ 多人同时说话 | 超出本科毕设规模，业界也是独立难题 |
| 歌声、音乐 MV、纯 BGM 无解说视频 | ASR 无有效文本，场景不同 |
| 强噪声 / 远场录音 / 重口音方言 | 前置假设即"人声清晰" |
| **BGM 保留混音** | 需人声分离（Demucs 类），列为未来工作；素材选无 BGM 或低 BGM |
| 视频生成 / 剪辑 / 转场等创作能力 | 本项目只"翻译"，不改画面内容 |
| 实时 / 流式翻译 | 批处理定位；流式是另一套架构 |
| 4K、> 5 分钟长视频、原硬字幕擦除 | 规模与算力边界，列为未来工作 |

### 4.3 规模限制

- 单视频 ≤ 5 min、≤ 200MB、≤ 200 段；并发 = 1（单机单任务，不做任务队列）
- **评测集**：自制素材 5 条（每条 15–60s，`make_sample.py` 扩展，不用真实品牌广告）：
  ① 纯口播 ② 短句密集 ③ 带低音量 BGM ④ 人脸出镜（lip-sync 用）⑤ 长句慢语速

### 4.4 硬件环境

| 设备 | 角色 |
|------|------|
| macOS（Apple Silicon，无独立 GPU） | 开发机 + 主流水线：ASR 走本地 CPU 或云端 API，TTS/LLM 全走 API |
| Windows 服务器（RTX 5090 + 9950X，自有） | 仅 LatentSync 推理，局域网 HTTP（0.0.0.0:8001） |

不采购任何付费算力；不租 GPU。

### 4.5 推荐软件环境

- macOS：Homebrew ffmpeg ≥ 8.0；Python 3.13 venv；pytest；git 按主题提交
- Windows：按 `server/README_WINDOWS.md` 装 Python + cu128 PyTorch + LatentSync
- 前端：Node ≥ 20 + Vite + React
- 配置：`.env`（`LLM_API_KEY` / `OPENROUTER_API_KEY` / `LIPSYNC_SERVER_URL` 等）

---

## 5. 中间里程碑（目的 → 验收）

| 里程碑 | 内容 | 验收标准 | 状态 |
|--------|------|----------|------|
| **M0** 仓库规整 | 首次 commit、.gitignore 修整、GOAL.md 入库 | git log 有按主题的提交历史 | 本周 |
| **M1** 核心流水线 | asr→翻译→tts→对齐→合成，CLI 一条命令 | 样例视频出片；单测全绿 | ✅ 2026-09-04（13.3s 出片，10 测试绿） |
| **M2** lip-sync 打通 | 5090 跑通 LatentSync → 起服务 → 人脸检测分段 → `--lipsync` 开关 | Mac 一条命令出 15s 口型正确样片；30s 真人口播成品口型基本同步 | ~2 周 |
| **M3** 产品面 | FastAPI 三端点 + SQLite 任务表 + React 拖拽上传页 | 非技术人员不看文档完成一次翻译，浏览器播放成品 | ~1 周 |
| **M4** Agent/MCP 改造（求职差异点） | 各阶段注册为 MCP 工具，Agent 读 report.json 自主决策重译/换供应商 | Agent 版溢出数 ≤ 规则版；决策日志可讲 | ~1 周（毕设加分项，时间紧可降级） |
| **M5** 评测体系 | 评测脚本自动算 §6 全部指标 + 完成 §8 实验 | 5 条素材一键跑出完整指标表 | ~1 周 |
| **M6** 包装交付 | README（架构图+GIF+Dify 截图）、1–2 分钟对比视频、答辩 demo 脚本 | 新机器按 README ≤ 30 分钟复现出片 | ~1 周 |

---

## 6. 结果质量怎么测（每条成品的指标）

### 6.1 客观指标（评测脚本自动计算）

| 指标 | 定义 | 合格线 | 目标线 |
|------|------|--------|--------|
| 溢出率 | `action=overflow` 段占比（重译后） | ≤ 5% | 0% |
| 时长匹配率 | `fit + atempo` 段占比 | ≥ 95% | 100% |
| ASR 字错率 CER | 对带文稿素材，中文字符错误率 | ≤ 5% | ≤ 2% |
| 字幕时间轴偏移 | 成品字幕起点 vs 源语音起点 | ≤ 200ms | ≤ 100ms |
| 口型可用率 | 人脸段人工判定"口型可接受"比例 | ≥ 80% | ≥ 95% |
| 端到端耗时 | 处理时长 / 视频时长（lip-sync 关 / 开） | ≤ 10× / 20× | ≤ 5× / 15× |
| 单条成本 | 30s 广告 API 成本分项合计 | ≤ ¥1 | ≤ ¥0.5 |

### 6.2 主观 / 半自动指标

- **配音自然度 MOS**：5 分制，≥ 3 名评委，合格 ≥ 3.5
- **翻译质量**：LLM-as-a-Judge（忠实度 / 流畅度 / 简洁度，1–5 分）合格 ≥ 4.0；
  随机抽 20% 人工复核评委一致性
- **多语言泛化**：ja / es 各 1 条全链路跑通、成片可看懂

---

## 7. 系统合格标准（Definition of Done，全部满足才算合格）

1. **全链路**：5 条评测素材批量跑通，成功率 5/5，报告自动生成
2. **质量**：§6.1 表全部达合格线（允许 1 项未达目标线，需注明原因）
3. **可用性**：3 名未接触过项目的同学，不看文档，5 分钟内经网页各自完成一次翻译（≥ 2/3 成功）
4. **健壮性**：无声视频 / API 失败 / 超长段均有处理路径；pytest 全绿；
   断点续跑实测（中途 kill 后重跑不重复调付费 API）
5. **可复现**：新机器按 README ≤ 30 分钟部署并出片
6. **可讲述**：毕设——论文六章素材齐全；求职——简历 3–4 条量化 bullet，
   每条背后有 5 分钟深挖的技术故事

---

## 8. 实验设计（论文第 5 章 / 简历数字的唯一来源）

| 实验 | 设计 | 产出结论 |
|------|------|----------|
| **E1** 音画同步消融 | {无限长, 仅限长, 限长+atempo, 限长+atempo+重译} × 5 素材 | 溢出率/匹配率对比表，结论形如"溢出率 X% → 0%"（跑完填数） |
| **E2** TTS 选型 | edge-tts vs fish-audio：MOS、时长匹配率、耗时、成本 | 供应商选型依据 |
| **E3** lip-sync 方案 | 自建 LatentSync vs sync.so：人工评分 + SyncNet 分数、耗时、价格 | "自建 GPU vs 商用 API"取舍结论 |
| **E4** 多语言泛化 | en / ja / es 各 1 条全指标对比 | 方法跨语言有效性 |
| **E5** 成本核算 | 30s 广告单条成本分项（ASR / LLM / TTS / lip-sync） | "单条 ¥X"简历数字 |

---

## 9. 与两套验收标准的映射

- **毕设**（题目建议：《基于大语言模型与语音合成的视频翻译配音系统设计与实现》）：
  M0–M3 + M5 + M6 必做；M4 为加分项。
  论文章节：需求分析 → 系统设计（ARCHITECTURE.md 改写）→ 关键算法（音画同步双向夹逼）→
  系统实现 → 实验与评测（§8）→ 总结与展望（§4.2 出界项即"展望"素材）。
- **求职**（目标岗：AI 应用开发 / 大模型应用工程岗，非算法岗）：
  M4（Agent/MCP）是简历差异点；对齐 JD 关键词——Agent 开发、流程编排、
  评测体系（LLM-as-a-Judge）、多模态、工程化。
